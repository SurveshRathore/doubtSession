// simple function
var x = function(x, y) {
    return x * y;
  }

  console.log("simple function", x(4,5));
  
  // arrow function
  const arrowX = (x, y) => x * y;

  console.log("arrow function", arrowX(4,5));
